
create an ui project using bun, tanstack router, shadcn.
create a login screen with user and password as a initial public route.
create the dashboard screen as defautl router when the user log into the app.
create a devices, stages,  protocols, and tracking screens as privete routes. These screens need to list the related records returned
from the api as defined in aopenapi.json file.

create an auth api that receive user and password and validate them against a env vars. after that configure login page request this new auth api.

make modification on device screen to edit a device. use as reference the openapi.json file as api. 
the user can change the name and the stage which is a selection box that retrieves the available stages from api.


ssh auto-grow-api

### Bare Repo ###
-- create remote repo --
sudo mkdir -p /srv/git
sudo chown -R ubuntu:ubuntu /srv/git
cd /srv/git
git init --bare auto-grow-api.git

-- create app dir --
mkdir -p /var/www/auto-grow-api

-- Add post-receive hook --
cd /srv/git/auto-grow-api.git/hooks
nano post-receive

#!/bin/bash
set -e

echo "starting deploy..."

APP_DIR=/var/www/auto-grow-api
GIT_DIR=/srv/git/auto-grow-api.git

git --work-tree=$APP_DIR --git-dir=$GIT_DIR checkout -f

cd $APP_DIR/api
bun install --production
bun run compile

#pm2 restart auto-grow-api || pm2 start index.js --name auto-grow-api
#pm2 save

-- add permission --
chmod +x post-receive

-- configure git remote --
git remote add production  ssh://auto-grow-api:/srv/git/auto-grow-api.git

-- deploy --
git push production main
ssh auto-grow-api

export NODE_ENV=production
export API_USERNAME=admin
export API_PASSWORD=admin
export DB_PATH=/Users/luissanches/Projects/auto-grow/api/data/auto-grow.db



nohup PORT=8080 bun index.html > bun.log 2>&1 &

pm2 start "PORT=8080 bun index.html" --name bun-static
pm2 save